# queryset API methods that returns new QuerySets and that do not return QuerySets both  we mention in app 
## in views section there is note of all method

# QuerySet API Field lookups in fieldlookups app
